'use client'; import {useState} from 'react'; import Link from 'next/link'; export default function Settings(){const[msg,setMsg]=useState('');async function logout(){await fetch('/api/auth/logout',{method:'POST'});location.href='/login'}return <main className="shell"><header className="topbar"><div className="brand"><span className="seed">🌱</span>MindBloom</div><nav className="nav"><Link href="/dashboard">Home</Link></nav></header><div className="dashboard"><h1>Settings</h1><div className="card"><h2>Account</h2><p className="muted">Manage your MindBloom session and family learning space.</p><button className="btn secondary" onClick={logout}>Log out</button>{msg&&<p>{msg}</p>}</div><div className="card"><h2>Safety & privacy</h2><p className="muted">Use a strong password. Keep private family information out of activity notes unless it is necessary for your care team.</p></div></div></main>}
