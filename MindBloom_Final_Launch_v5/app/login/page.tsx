"use client";import {FormEvent,useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";export default function Login(){const r=useRouter();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const x=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const j=await x.json();if(!x.ok){setError(j.error||'Could not log in');setBusy(false);return}r.push(j.role==='OWNER'?'/owner':'/dashboard');r.refresh()}return <main className="login"><div className="brand"><span className="seed">🌱</span>MindBloom</div><h1>Welcome back</h1><p className="muted">Continue your child's learning journey.</p><form onSubmit={submit}><label>Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/><label>Password</label><input className="input" value={password} onChange={e=>setPassword(e.target.value)} type="password" required/>{error&&<p className="error">{error}</p>}<button className="btn" disabled={busy}>{busy?'Signing in…':'Log in'}</button></form><p className="muted">New here? <Link href="/signup" style={{color:'#197e66',fontWeight:700}}>Create an account</Link></p></main>}
