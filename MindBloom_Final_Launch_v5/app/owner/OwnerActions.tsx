"use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function OwnerActions({id}:{id:string}){const r=useRouter();const[busy,setBusy]=useState(false);async function act(action:'approve'|'reject'){setBusy(true);const reason=action==='reject'?window.prompt('Reason (optional):')||'Rejected by owner':undefined;const x=await fetch('/api/owner/payments/'+id,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,reason})});if(!x.ok)alert((await x.json()).error||'Could not process');r.refresh();setBusy(false)}return <div style={{display:'flex',gap:8}}><button className="btn" disabled={busy} onClick={()=>act('approve')}>Approve</button><button className="btn secondary" disabled={busy} onClick={()=>act('reject')}>Reject</button></div>}
