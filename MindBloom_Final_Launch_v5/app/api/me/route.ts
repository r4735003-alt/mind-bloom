import {NextResponse} from 'next/server'; import {getSession} from '@/lib/auth'; import {prisma} from '@/lib/prisma'; import {getEntitlement} from '@/lib/entitlements';
export async function GET(){const s=await getSession();if(!s)return NextResponse.json({error:'Unauthorized'},{status:401});const user=await prisma.user.findUnique({where:{id:s.userId},select:{id:true,email:true,name:true,role:true,createdAt:true,children:{orderBy:{createdAt:'asc'}}}});if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const goals=await prisma.goal.findMany({where:{child:{parentId:s.userId}},orderBy:{createdAt:'desc'}});return NextResponse.json({...user,goals,entitlement:await getEntitlement(s.userId)});}
