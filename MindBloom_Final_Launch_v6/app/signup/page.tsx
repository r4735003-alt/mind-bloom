"use client";import {FormEvent,useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";const bands=['2–4 years','4–6 years','6–9 years','9–12 years','12–15 years','15–18 years','18+ years'];export default function Signup(){const r=useRouter();const[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[ageBand,setAgeBand]=useState(bands[0]),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const x=await fetch('/api/auth/signup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password,ageBand})});const j=await x.json();if(!x.ok){setError(j.error||'Could not create account');setBusy(false);return}r.push('/dashboard');r.refresh()}return <main className="login"><div className="brand"><span className="seed">🌱</span>MindBloom</div><h1>Create your account</h1><p className="muted">Your first 31 days are free.</p><form onSubmit={submit}><label>Your name</label><input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Parent / caregiver name" required/><label>Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" required/><label>Password</label><input className="input" value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} required/><label>Child's age band</label><select className="input" value={ageBand} onChange={e=>setAgeBand(e.target.value)}>{bands.map(x=><option key={x}>{x}</option>)}</select>{error&&<p className="error">{error}</p>}<button className="btn" disabled={busy}>{busy?'Creating…':'Create account →'}</button></form><p className="muted">Already have an account? <Link href="/login" style={{color:'#197e66',fontWeight:700}}>Log in</Link></p></main>}
