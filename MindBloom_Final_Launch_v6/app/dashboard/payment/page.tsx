"use client";import Link from "next/link";import {FormEvent,useState} from "react";export default function Payment(){const[trx,setTrx]=useState(''),[msg,setMsg]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');setMsg('');const x=await fetch('/api/payments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({trxId:trx})});const j=await x.json();if(!x.ok){setError(j.error||'Could not submit');setBusy(false);return}setMsg('Submitted. The owner will verify your payment.');setTrx('');setBusy(false)}return <main className="login"><div className="brand"><span className="seed">🌱</span>MindBloom</div><h1>Activate 31 days</h1><p>Transfer <b>Rs.799</b> through NayaPay.</p><div className="card"><b>Muhammad Raffy Umer</b><br/>03250150477</div><form onSubmit={submit}><label>TRX ID</label><input className="input" value={trx} onChange={e=>setTrx(e.target.value)} placeholder="Enter your transaction ID" required/>{error&&<p className="error">{error}</p>}{msg&&<p className="success">{msg}</p>}<button className="btn" disabled={busy}>{busy?'Submitting…':'Submit for verification'}</button></form><p className="muted">Approval activates exactly 31 days from the owner's approval timestamp.</p><Link href="/dashboard">← Back to dashboard</Link></main>}
